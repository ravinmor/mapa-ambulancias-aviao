# Controle — Aeronave Monitorada (Amil)

Checklist de controle pra tudo relacionado ao rastreio da aeronave específica
da Amil: o rastreador em si (`/aviacao-executiva` neste projeto) e a
apresentação dele no Command Center (LED wall, `command-center-cloudflare`).

**Como usar este arquivo:** cada task tem um ID (ex: `R-07`, `P-A2`). Ao
concluir uma, marcar `[x]` e mover pra baixo do "Concluído" da seção
correspondente, sem apagar o ID. Não remover tasks bloqueadas — deixar
marcadas como bloqueadas até a decisão chegar.

Última atualização: 2026-09-08.

---

## Seção 1 — Rastreador (`/aviacao-executiva`)

### Concluído
- [x] `R-01` Pipeline paralelo por ICAO24 fixo (`sync-job/src/trackedAircraft.ts`), independente do genérico do OpenSky
- [x] `R-02` Pipeline genérico (SP/RJ, vagas) mantido no código, só desligado
- [x] `R-03` Endpoint `GET /api/tracked-aircraft`
- [x] `R-04` Rota `/aviacao-executiva` com mapa de design próprio
- [x] `R-05` Marcador da aeronave (ícone, rotação pelo `trueTrack`)
- [x] `R-06` Cálculo de fase do voo (7 estágios fixos: Solo → Decolagem → Subida → Cruzeiro → Descida → Aproximação → Pouso)
- [x] `R-07` Arco de linha do tempo estilo SpaceX (fade nas bordas, estágio atual centralizado, sem contador T+/-)
- [x] `R-08` Build validado rodando com dado real (avião de teste em cruzeiro)
- [x] `R-09` Badge "ao vivo" — trocado pelo mesmo estilo/componente já usado no mapa das ambulâncias (`.status-badge` + `.live-dot`), com o texto simplificado pra só "ao vivo"
- [x] `R-10` Arco da linha do tempo — curvatura corrigida (era parábola achatando perto do vértice; virou círculo de verdade, mesmo centro+raio sempre, curvatura visível não importa qual estágio está ativo)
- [x] `R-11` Avião não estava se movendo — causa real: página não usava `useDeadReckoning` (o mapa genérico já tinha isso). Generalizei o hook e apliquei — confirmado ao vivo, marcador se move sozinho entre buscas reais, sem chamada nova
- [x] `R-23` Suporte a múltiplas aeronaves — `TrackedAircraftConfig.icao24List` (lista, não mais 1 ICAO24 fixo), `sync-job` roda 1 ciclo por aeronave com intervalo adaptativo próprio (parada: 15min / voando: 5min) e busca por caixa pequena ao redor da última posição conhecida (1 crédito, com resgate global se não achar). Testado com 4 aeronaves de placeholder simultâneas
- [x] `R-12` Trajeto (rastro) da aeronave no mapa — schema/migration `TrackedAircraftPositionHistory` espelhado (sync-job + api), `GET /api/tracked-aircraft/:id/history` (mesmo padrão de corte por gap do genérico, janela maior: 48h/45min), `AircraftTrail.tsx` reaproveitado com o mesmo gradiente de cor por altitude, ponto final sempre a posição ao vivo (dead reckoning) pra não atrasar em relação ao marcador. Ícone e pulsar do marcador também passaram a usar a mesma cor por altitude (antes só o rastro)

- [x] `R-14` Mira no mapa: linha horizontal + vertical cruzando na posição em tela da aeronave selecionada (acompanha dead reckoning + pan/zoom via `latLngToContainerPoint`), com latitude/longitude pequenininhas do lado de cada linha (`AircraftCrosshair` em `AmilJetPage.tsx`). Só aparece com uma aeronave selecionada
- [x] `R-13` No arco: traço removido (pedido do usuário, 2026-09-02: "só remova o traço"). 2ª parte ("ícone indicando onde estamos") resolvida pelo diamante da bússola (R-24, pedido do usuário 2026-09-03) — cumpre o mesmo papel de marcador de posição atual, sem precisar de um ícone de avião separado

### Falta — enriquecimento de dados (pedido 2026-09-02)
- [x] `R-15` Sistema de monitoramento de squawk — `squawk` buscado no OpenSky (`trackedAircraftSource.ts`), coluna nova (migration + schema espelhado sync-job/api), exposto na API e no tipo do frontend. Alerta visual nos 3 códigos de emergência: marcador vira vermelho (`--color-alert-400`) com pulso rápido (0,6s) que vence até o estado offline — visível no mapa sem precisar selecionar — e banner vermelho na topbar com código + significado quando a selecionada está em emergência
- [x] `T-02` (parcial, só squawk) Mecanismo de injeção de teste — `sync-job/scripts/set-squawk.mjs`, escreve direto no Postgres via Prisma (usuário escolheu "script/CLI separado" em resposta à `Q-6`, 2026-09-02). `isAboutToDepart` (a outra metade do T-02) ainda não existe — depende da Seção 2 (Command Center)
- [ ] `R-16` Fonte da posição (ADS-B / MLAT / etc.) com tooltip explicando o que significa ao passar o mouse
- [ ] `R-17` Metadado da aeronave (fabricante/modelo/operador) via endpoint público de metadados do OpenSky (`/api/metadata/aircraft/icao/{icao24}`) — **gratuito, confirmado**
- [ ] `R-18` Origem/destino do voo — **não vem do ADS-B**; vem do sistema de agendamento (ver `Q-1`, respondida 2026-09-08) — falta o acesso pra implementar. **Coexistência com o adsbdb resolvida (2026-09-08, sem conflito)**: hoje já existe um "destino" via adsbdb (rota comercial resolvida pelo callsign, ver `AmilDestinationLine`/`useDeadReckoning.ts`) — o OpenSky dá telemetria igual pra avião ou helicóptero, mas o adsbdb só resolve ROTAS COMERCIAIS cadastradas (aeroporto-aeroporto). Helicóptero indo pro heliponto de hospital não é rota comercial — adsbdb já fica `null` nesse caso hoje. Não há sobreposição real: adsbdb cobre aviões em rota comercial, o destino do agendamento cobre as missões da Amil (helicóptero→hospital principalmente, mas também avião→aeroporto). Quando `R-18` existir, usar o destino do agendamento quando houver missão ativa; adsbdb continua sendo o fallback pra quando não há
- [ ] `R-19` Cidade/região sobrevoada — geocodificação reversa do lat/lon (ex: Nominatim/OSM, gratuito com limite de uso — confirmar se o limite serve pro nosso volume de chamadas)
- [ ] `R-20` Clima na rota/destino (METAR/TAF do aeroporto mais próximo) — depende de `R-18` existir primeiro (precisa saber o destino pra buscar o clima de lá)
- [x] `R-21` Horário de início/fim do voo atual (ou do último, enquanto parado) + cronômetro de tempo de voo decorrido ao vivo na topbar, formato `MM:SS:mmm` em fonte digital (Share Tech Mono, pedido do usuário: "fonte de calculadora, números digitalizados"), tickando a cada 50ms num componente isolado (`FlightElapsedReadout`, não re-renderiza a página inteira). **2 bugs reais corrigidos em teste ao vivo (2026-09-03)**: (1) `resolveFlightTiming()` usava `existing.onGround` bruto (reescrito todo ciclo, sem relação com confirmação) pra decidir "já confirmado voando" — a 1ª leitura no ar já grava `onGround:false` no upsert principal, então a 2ª leitura (que deveria CONFIRMAR o candidato) lia esse valor e concluía que já tinha confirmado antes, pulando a confirmação pra sempre. Reproduzido com o TAM3144 decolando de verdade. Corrigido usando só `flightStartedAt`/`flightEndedAt` (nunca `onGround`) como fonte de verdade do estado confirmado. (2) **Pouso ficava preso sem confirmar pra sempre** quando a aeronave sumia de sinal logo após pousar (motor desligado = normal, não exceção) — a confirmação exigia uma 2ª leitura real que nunca chegava. Reproduzido com o GLO7640 (usuário: "está parado a muito tempo e o contador continua"). Corrigido: sumir de sinal com um pouso pendente de confirmação AGORA confirma o `flightEndedAt` direto (silêncio pós-pouso é o próprio sinal de confirmação); intervalo de checagem também passou a continuar rápido (5min, não 15) enquanto há qualquer transição pendente, pra confirmar o mais perto possível do momento real. Testado ao vivo: GLO7640 confirmou pouso corretamente (13:51→15:57, "Último voo" congelado, não mais contando). **Bug 3**: `wasFlying` exigia `isOnline===true` antes de checar a pendência — aeronave já offline com pouso pendente (o caso exato que o fix resolve) caía de novo no intervalo lento. Corrigido: pendência força intervalo rápido independente de `isOnline`. Cronômetro também foi movido do topbar pra embaixo da bússola, centralizado na área de instrumentos (pedido do usuário, 2026-09-03). **Detecção robusta a pedido do usuário** ("o Command usará isso pra criar um alerta, precisa ser robusto"): `flightStartedAt`/`flightEndedAt` só gravam depois de **2 leituras reais consecutivas confirmando** — 1 leitura isolada de `on_ground` ruidoso vira só um candidato (`pendingDepartureAt`/`pendingArrivalAt`, colunas internas, não expostas pela API) e é descartada sem alarme se a próxima não confirmar. "Parece no ar" exige `onGround:false` E (altitude > 30m OU velocidade > 20m/s) — filtra o caso mais comum de leitura ruim (taxi/ruído com on_ground oscilando perto de zero). Ver `resolveFlightTiming()` em `sync-job/src/trackedAircraft.ts`, separado de propósito de `deriveStage()` (que continua reagindo numa leitura só, por ser só cosmético pro arco). Testado com timestamp forçado direto no banco — contador tickando ao vivo confirmado (5:50→5:53 em 3s)
- [ ] `R-22` Mini-gráfico de altitude ao longo do tempo (depende de `R-12`, o histórico de posição/altitude)
- [x] `R-24` Bússola (HSI simplificado) — anel de graus (cardeais N/L/S/O maiores, colaterais NE/SE/SO/NO menores, ambos acima do tick + número do grau embaixo) gira conforme o `trueTrack`, dentro do MESMO SVG/máscara de fade da linha do tempo (não componente separado — o oposto do `AmilArcBackdrop`, aqui o fade compartilhado é o comportamento certo). Diamante entre os 2 anéis marcando a posição atual (ponta de cima aponta pro arco de estágios, ponta de baixo pra bússola — ver `R-13`). Curvatura da linha do tempo também ajustada pra ficar mais parecida com a da bússola (`RADIUS` em `AmilTimelineArc.tsx`, parâmetro documentado pra ajuste fino)
- [x] `R-25` Altímetro em fita vertical (canto inferior esquerdo, só com seleção) — cor de cada traço = `altitudeColor()` (mesma função do marcador/rastro), números dos 2 lados a cada 500m. Aparece TAMBÉM pousada (altitude tratada como 0, parte de baixo mostra negativos — ADS-B não manda altitude no chão). 2 setas fixas no centro (referência "nivelado") + glifo de asas amarelo MÓVEL baseado na taxa vertical em m/s — acima subindo, abaixo descendo, alinhado nivelado. Testado com valor forçado (-8m/s → glifo 30.7px abaixo das setas, batendo com o cálculo esperado)
- [ ] `R-26` Linha de design geral da página seguindo referência "Aerovista" (fornecida pelo usuário, 2026-09-03): tema azul-marinho escuro, cards com cantos arredondados grandes, tags de status coloridas pequenas, gráfico de linha fino sem grade pesada — aplicar de forma consistente conforme os itens acima (R-21/R-24/R-25) forem implementados, não como task isolada
- [x] `R-27` Painel "Fleet Status" expansível na topbar (`AmilFleetStatus.tsx`, pedido do usuário 2026-09-03, referência visual fornecida) — card com foto + nome/serial + pílula de status colorida (Em missão/Disponível/Pousado, 3 categorias em vez das 4 da referência), derivado do mesmo `stage` que já alimenta o arco (SOLO=Disponível, POUSO=Pousado, resto=Em missão, sem dado novo). Fotos: reaproveitadas de `vehiclePhotos.ts` (mesmo par avião/helicóptero do mapa genérico) — 2 das 4 viram helicóptero, escolha determinística por hash do icao24 (nova função `pickHelicopterIcaosFlat`, mesma técnica de `pickHelicopterIcaos` já existente, generalizada pra lista sem região)

### Redesenho de layout (pedido do usuário, 2026-09-03 — lista de 7 itens)
Passo a passo definido pelo usuário: 1) traços laterais decorativos; 2) câmera trava no avião selecionado; 3) rótulo flutuante com ICAO+ANAC seguindo o avião; 4) reorganização esquerda(metadados)/direita(voo+indicadores); 5) marcador se move suave (sem piscar); 6) crosshair se move suave; 7) crosshair afina nas bordas e engrossa perto do avião.

#### Concluído
- [x] `R-28` Traços laterais decorativos (item 1) — `AmilSideTicks.tsx`, régua de traço grande + traço pequeno intercalados nas 2 bordas do mapa, sempre visível (não depende de seleção). Vários ajustes em resposta a feedback visual direto: traço em `<pattern>` SVG trocado por render explícito em JS (`ResizeObserver` na altura real do container) pra poder diferenciar o primeiro/último traço (maiores que os do meio) e garantir que a sequência sempre termine numa marca grande, não numa pequena pendurada; ponto trocado por traço pequeno; ponto/traço pequeno alinhado com a ponta externa do traço grande (início à esquerda, fim à direita); margem lateral fixa de 12px da borda real da tela + régua nunca encosta nela; margem vertical de 20px no topo/base implementada com `padding` num `<div>` pai (não no `<svg>` direto — svg é elemento "replaced", não estica de forma confiável só com `top`/`bottom`, foi o que quebrou em 2 tentativas anteriores). Fade preto nas laterais (`.amil-side-fade`) e no topo (`.amil-top-fade`, extraído do background do `.amil-topbar` pra ficar no mesmo nível) — ambos atrás dos traços (`z-index` abaixo de `--z-frame`). Regra de camadas formal criada (`--z-frame` < `--z-hud` < `--z-controls`, variáveis CSS em `:root`) e aplicada em todos os elementos sobrepostos ao mapa, incluindo os controles nativos do Leaflet (zoom), que ganharam margem lateral (`--amil-content-inset`, 20px do limite externo dos traços) e vertical (34px do fundo, alinhando com a marca grande de baixo da régua). Fade lateral (`.amil-side-fade`) ampliado depois (pedido do usuário 2026-09-03: "aumente ainda mais o fade e deixe ligeiramente mais escuro") pra cobrir toda a largura do altímetro, não só o respiro dos traços — padronizado em `24vw` de cada lado (era um valor fixo em px, não escalava com a largura da tela)

#### Concluído (cont.)
- [x] `R-29` Câmera trava no avião selecionado (item 2) — novo componente `CameraFollowSelected` dentro do `MapContainer` (`AmilJetPage.tsx`), reage a cada mudança de lat/lon do selecionado (inclusive as posições estimadas por dead reckoning, a cada 1s) e usa `panTo` (sem mudar zoom) pra manter a câmera sempre em cima dele, de forma animada — não um salto. Ignora enquanto `isFocusing` é true, pra não brigar com o `flyTo` inicial do clique. Testado ao vivo: arrastei o mapa manualmente pra longe do avião selecionado, a câmera recentralizou sozinha nele no próximo tick. **Refinamentos** (pedido do usuário, 2026-09-03): (1) arrasto manual do mapa (`dragstart` do Leaflet, só dispara em interação real do usuário — nunca em `panTo`/`flyTo` programático) agora PAUSA o travamento por 30s antes de voltar a seguir sozinho; (2) botão próprio de recentralizar (`L.Control` customizado, ícone de mira) no canto inferior direito, empilhado junto do zoom — força volta imediata + destrava; (3) botões de zoom in/out lado a lado (linha) em vez de empilhados (escopado a `.amil-page`, mapa genérico das ambulâncias continua empilhado). **Mais 4 correções** (pedido do usuário, 2026-09-03): travamento agora reativa automaticamente ao trocar de avião selecionado (`key={selectedId}` força o componente remontar, zerando a pausa herdada da seleção anterior); borda azul residual embaixo do botão "+" removida (sobra do CSS padrão do Leaflet pra layout empilhado); botão de recentralizar movido pra ficar do lado ESQUERDO do zoom (não mais em cima) — canto inferior direito agora é `display:flex;flex-direction:row-reverse` com os 2 controles alinhados no mesmo nível (`padding-bottom:34px` no container, batendo com o traço maior final da régua); avião selecionado sempre com `zIndexOffset` maior que os demais marcadores (Leaflet por padrão ordena por latitude, podia deixar o selecionado atrás de outro mais ao sul)

- [x] `R-17` Metadado da aeronave via endpoint público de metadados do OpenSky (`/api/metadata/aircraft/icao/{icao24}`) — implementado junto do `R-30` (dependência dele). `fetchAircraftRegistration()` em `sync-job/src/sources/trackedAircraftSource.ts`, buscado 1 ÚNICA vez por aeronave (não muda, endpoint separado/público, sem autenticação) e cacheado na coluna nova `registration` (migration espelhada sync-job/api). Só fabricante/modelo/operador (o resto do endpoint) ficam de fora por enquanto — não pedidos
- [x] `R-30` Rótulo flutuante ICAO24 + matrícula (item 3 do redesenho) — `AmilAircraftLabel` em `AmilJetPage.tsx`, mesma técnica de posicionamento do `AircraftCrosshair` (`latLngToContainerPoint` a cada posição/pan/zoom/resize), canto superior direito da aeronave com linha guia curta, estilo de card conforme referência visual fornecida (dot + título + subtítulo). Aparece sempre pra a aeronave SELECIONADA e, pra qualquer OUTRA, no hover do mouse (novo estado `hoveredId`, wired via `eventHandlers.mouseover/mouseout` nos marcadores). Hover também aumenta o avião de tamanho e pulsa: `scale` vai INLINE junto do `rotate()` já existente no SVG (uma animação CSS de `transform` substituiria a rotação — mesmo motivo pelo qual o pulso normal do projeto só anima `filter`, nunca `transform`), suavizado por `transition: transform` no CSS; o pulso em si é um novo `@keyframes amil-jet-hover-pulse` (só `filter`, mesma técnica seleprovada de `amil-jet-pulse`). Testado ao vivo via evento sintético de mouseover: 2 rótulos simultâneos (selecionado + hover), `transform` confirmado como `rotate(...) scale(1.3)` sem perder a rotação

- [x] `R-32` Marcador se move de forma suave entre posições (item 5) — `.amil-jet-marker { transition: transform 1s linear; }` no wrapper que o Leaflet já posiciona via `transform` inline a cada tick do dead reckoning (1s); elemento DIFERENTE do svg de dentro (que tem seu próprio `transform` de rotação/hover), sem conflito. Confirmado via `getComputedStyle` (`transitionProperty: "transform"`, `transitionDuration: "1s"`)
- [x] `R-33` Crosshair se move de forma suave (item 6) — **REVERTIDO** logo em seguida (pedido do usuário, 2026-09-03: "remova a transição suave do crosshair ficou muito devagar") — `AircraftCrosshair` voltou a acompanhar a posição instantaneamente, sem `transition`. Durante a implementação (antes de reverter) foi encontrado e corrigido um bug real: os 2 segmentos que usavam `right:0`/`bottom:0` (largura/altura implícitas, calculadas pelo layout) faziam o navegador interpolar valores absurdos (milhões de px) no meio de uma transição CSS de tamanho — a correção (width/height EXPLÍCITOS em JS pros 4 segmentos, novo estado `mapSize` via `map.getSize()`) ficou no código mesmo com a transição removida, é mais robusta de qualquer forma
- [x] `R-34` Linhas do crosshair afinam nas bordas e engrossam perto do avião (item 7) — cada segmento é uma caixa (4px de espessura máxima, pedido do usuário 2026-09-03: "deixe bem mais fino próximo da aeronave" — era 10px) com `clip-path: polygon(...)` recortando uma cunha: fina no lado da BORDA da tela, cheia no lado do VAO (perto do avião), direção espelhada nos 4 segmentos

- [x] `R-31` Painel de metadados no lado ESQUERDO (item 4, ÚLTIMO item do redesenho de layout de 7 itens — **redesenho concluído**) — `AmilMetadataPanel.tsx`, estilo baseado em referência visual fornecida pelo usuário 2026-09-04 (dashboard DATAV de aeroporto): moldura fina + "brackets" em L nos cantos superiores (`.amil-hud-panel`, classe genérica reutilizável), ícone do avião + grid 2x2 de dados. **Achado crítico durante a implementação**: o endpoint de metadados do OpenSky que alimentaria matrícula/fabricante/modelo/operador (`R-17`) foi **descontinuado pela OpenSky** (410 Gone, confirmado com e sem autenticação, 2026-09-04) — não é falha temporária. Decisão do usuário: construir o painel mesmo assim, só com dados REAIS que já temos (ICAO24, matrícula — campo existe, fica "—" até haver fonte de novo —, fase, última posição conhecida), sem inventar fabricante/modelo/operador que nunca teriam dado real. Lado direito (voo + indicadores) já estava concluído desde o `R-25` (altímetro). **Complemento** (pedido do usuário, 2026-09-04: "crie mais um painel igual esse com as informações da navbar. Aproveite e remova as informações da navbar deixando só a frota"): novo `AmilFlightInfoPanel.tsx`, mesmo estilo `.amil-hud-panel`, empilhado ACIMA do altímetro (mesmo grupo animado) com Altitude/Velocidade/Rumo/Taxa vertical — essas 4 métricas + a identidade (nome/ICAO) saíram da topbar (ficaram redundantes com os 2 painéis novos). **Correção** (pedido do usuário, 2026-09-04): o novo painel tinha ido pro lado ERRADO (direita) — movido pro lado ESQUERDO, empilhado junto do painel de metadados (mesmo grupo animado); o altímetro (instrumento visual, não card de dados) ficou sozinho à direita. O badge "ao vivo" tinha sido removido junto por engano — voltou pra topbar (não é "informação de rotina", é o único sinal de que os dados ainda estão chegando). Topbar final: Frota + emergência (crítico) + "ao vivo"

---

## Seção 2 — Apresentação no Command Center (LED)

Contexto: o mapa já aparece hoje na Face 3 do painel de LED, embutido via
iframe (`LedFaceThreeContent.tsx`, `command-center-cloudflare`), como 2
mapas lado a lado (São Paulo / Panorâmico). Esta seção adiciona um aviso de
decolagem iminente + uma visão em tela cheia da aeronave + uma faixa de
status no estilo ticker de bolsa de valores, no topo de todo o painel.

### Fase A — Sinal de "prestes a voar" (backend, mapa-ambulancias-aviao)
- [ ] `P-A1` **[AGUARDANDO ACESSO — ver `Q-1`/`Q-2`]** Critério definido (2026-09-08): cruzar horário estimado do sistema de agendamento com telemetria real (ADS-B/OpenSky) pra confirmar se a aeronave está voando por volta do horário agendado. Falta o acesso ao sistema de agendamento (mesmo padrão Power Automate) pra implementar — usuário vai levantar e passar
- [ ] `P-A2` Novo campo/endpoint expondo esse estado (ex: `isAboutToDepart` em `GET /api/tracked-aircraft`, ou endpoint próprio) — depende de `P-A1`
- [ ] `P-A3` Persistir se o alerta já foi disparado pra essa decolagem (evitar repetir o aviso a cada poll enquanto a condição continuar verdadeira)

### Fase B — Command Center recebe o sinal
- [ ] `P-B1` Rota de proxy no `worker/index.ts` pra consumir a API do mapa-ambulancias-aviao sem problema de CORS/mixed content
- [ ] `P-B2` Polling no componente pai das faces (não dentro do iframe da Face 3) pra checar esse status periodicamente

### Fase C — Aviso centralizado na Face 3
- [ ] `P-C1` Componente de aviso centralizado na Face 3 — **[aguardando `Q-3`]** animação a ser elaborada pelo usuário depois; por ora, placeholder simples
- [ ] `P-C2` Timer de 1 minuto de exibição
- [ ] `P-C3` Clique no aviso → abre a Fase D (mapa em tela cheia)
- [ ] `P-C4` Sem clique em 60s → aviso some e dispara a Fase E (faixa) automaticamente, com o texto "uma aeronave está decolando"
- [ ] `P-C5` Squawk de emergência (`R-15`: 7500/7600/7700) → abre a tela de mapa em tela cheia (Fase D) **sozinha**, sem esperar clique — pula direto a etapa do aviso centralizado/timer de 1min

### Fase D — Mapa em tela cheia na Face 3
- [ ] `P-D1` **[aguardando `Q-4`]** Transição "sci-fi" a ser elaborada pelo usuário depois; por ora, placeholder simples (ex: fade/slide)
- [ ] `P-D2` Mapa cobrindo toda a área da Face 3 (respeitando o tamanho da face, não a tela do LED inteira), substituindo os 2 mapas + resto do conteúdo da face
- [ ] `P-D3` Botão/gesto pra voltar à tela padrão de 2 mapas, disponível a qualquer momento (sem limite de tempo)
- [ ] `P-D4` Nesse modo, a faixa (Fase E) fica coberta **só na área da Face 3** — continua visível nas outras faces do painel
- [ ] `P-D5` Ao voltar pra tela padrão da Face 3, a faixa reaparece ali também

### Fase E — Faixa vermelha (ticker) no topo do painel inteiro
- [ ] `P-E1` Componente de ticker em nível de **painel inteiro** (acima/fora das faces individuais) — não é um elemento só da Face 3
- [ ] `P-E2` Estilo ticker de bolsa de valores: rolagem contínua horizontal, fundo vermelho, fonte pixelada
- [ ] `P-E3` Conteúdo inicial: aviso de decolagem; depois, atualiza pra status contínuo do voo até o pouso/fim
- [ ] `P-E4` Gatilho de aparição: ao clicar no aviso (`P-C3`) **ou** automaticamente após 1 min sem clique (`P-C4`) — os dois caminhos levam à faixa aparecendo
- [ ] `P-E5` Regra de sobreposição com `P-D4`/`P-D5` já cobre a interação com o mapa em tela cheia

---

## Seção 3 — Mecanismo de testes

Ferramentas pra validar o pipeline inteiro (rastreador + apresentação no LED)
sem depender de esperar uma decolagem de verdade ou uma emergência de
verdade acontecer.

- [ ] `T-01` **[aguardando `Q-5`]** Ferramenta pra localizar uma aeronave real prestes a decolar — consultar o OpenSky filtrando por área de aeroporto + `on_ground=true` + velocidade de solo subindo, listar candidatas, e permitir apontar `TRACKED_AIRCRAFT_ICAO24` pra uma delas. Serve pra validar end-to-end: `P-A1`/`P-A2` (sinal de "prestes a voar"), o aviso na Face 3, a faixa no LED, e a progressão real dos estágios (`R-06`) do início ao fim de um voo de verdade
- [ ] `T-02` **[aguardando `Q-6`]** Mecanismo de injeção de estado de teste — forçar um squawk específico (7500/7600/7700) ou o sinal `isAboutToDepart` sem que isso esteja acontecendo de verdade, pra validar os alertas visuais (`R-15` e a Fase C/E da Seção 2) sob demanda, sem esperar acontecer

---

## Perguntas em aberto (bloqueiam alguma task acima)

- **`Q-1`** — **[respondida, 2026-09-08]**: origem/destino vêm do sistema de agendamento existente (mesmo padrão SharePoint/Power Automate já usado pras missões das ambulâncias, ex. `POWER_AUTOMATE_MISSIONS_URL`/`POWER_AUTOMATE_REGULATIONS_URL` no `docker-compose.yml`) — pode ser aeroporto OU hospital (helicóptero pousa direto no heliponto do hospital). Ideia complementar do usuário: aeronave sem missão ativa deve ficar sempre em estado "pousada", na última posição de pouso salva (consistente com o comportamento que o sistema já tem — nunca apaga, sempre mantém última posição conhecida). **Bloqueado pra implementação**: usuário vai levantar o acesso (URL/credenciais/schema do fluxo) primeiro e chama quando estiver pronto — nenhuma integração deve começar antes disso.
- **`Q-2`** — **[respondida, 2026-09-08]**: "prestes a voar" não é só telemetria pura — cruza o horário ESTIMADO já cadastrado no mesmo sistema de agendamento (sujeito a atraso) com a telemetria real (ADS-B/OpenSky), checando se a aeronave está de fato voando por volta do horário agendado. Depende do mesmo acesso de `Q-1` pra implementar (`P-A1`/`P-A2`).
- **`Q-3`** (bloqueia o visual de `P-C1`): animação do aviso central — usuário vai elaborar depois
- **`Q-4`** (bloqueia o visual de `P-D1`): animação "sci-fi" da transição pro mapa em tela cheia — usuário vai elaborar depois
- **`Q-5`** (bloqueia `T-01`): a busca por aeronave prestes a decolar deve ficar restrita a algum aeroporto/base específico da Amil, ou vale qualquer aeroporto (facilita achar candidata, mas voo genérico não tem nada a ver com a Amil)?
- **`Q-6`** — **[respondida, 2026-09-02]**: script/CLI separado (`sync-job/scripts/set-squawk.mjs`). Ainda bloqueia a metade do `T-02` referente a `isAboutToDepart` (esse mecanismo cobriu só squawk).
